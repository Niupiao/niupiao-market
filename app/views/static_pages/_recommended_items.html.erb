<h2> Hey <%= current_user.first_name %>, Maybe These Items will Interest You? </h2>

<div class="row">
  <% tags = recommended_search_tags(current_user.id) %>
  <% items = internal_search(tags)%>
  
  <% items.each do |item| %>
  <div class="col-sm-3 col-lg-3 col-md-3">
    <div class="thumbnail">
      <img src="http://placehold.it/320x250" alt="">
      <div class="caption">
        <h4 class="pull-right">
          <%= item.price %>
        </h4>
        <h4><%= link_to item.name, item_path(item.id) %></a></h4>
        <small>Sold by <% if item.user %>
        <%= item.user.first_name %> <%= item.user.last_name %>
        <% end %> | <em><%= item.quantity %> left</em></small> 
          <p>
          <%= form_tag(cart_path(item_id: item.id)) do %>
            <%= select_tag "quantity", options_for_select((1..item.quantity).to_a, 1) %>
            <%= submit_tag("Add to cart", class: "btn btn-default") %>
          <% end %>
          </p>
    </div>
    <!--
    <div class="ratings">
      <p class="pull-right">X Reviews</p>
      <p>
          <span class="glyphicon glyphicon-star"></span>
          <span class="glyphicon glyphicon-star"></span>
          <span class="glyphicon glyphicon-star"></span>
          <span class="glyphicon glyphicon-star"></span>
          <span class="glyphicon glyphicon-star"></span>
      </p>
    </div>
    -->
    </div>
  </div>
  <% end %>
</div>