Cart:&nbsp;<%= session[:cart] %> <br>
<% session[:cart].each do |item_id, quantity| %>
    <% item = Item.find_by(id: item_id) %>
    <%= item.name %>
    <%= item.price %>
    <%= quantity %>
    <% end %>
